package Archiv

/*func loadTaskAll() {
	fmt.Print("\033[H\033[2J")
	fmt.Println("Alle Aufgaben anzeigen:\n############################################################################")

	AllList()

	fmt.Println("############################################################################")
	fmt.Println("\n[1] Neue Aufgabe erstellen, [2] Bearbeiten, [3] Löschen, [4] Beenden")

	newScan := bufio.NewReader(os.Stdin)

	for {
		fmt.Println("Wählen Sie eine Option: ")
		input, _ := newScan.ReadString('\n')
		input = strings.TrimSpace(input)
		Model.Check, _ = strconv.Atoi(input)

		switch Model.Check {
		case 1, 2, 3:
			SwitchChoose(Model.Check)
			break
		case 4:
			Menu()
			break
		default:
			fmt.Println("Ungültige Eingabe. Bitte versuchen Sie es erneut.")
			continue
		}
	}
}*/
