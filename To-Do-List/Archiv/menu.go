package Archiv

/*func Menu() {
	fmt.Print("\033[H\033[2J")

	fmt.Println("[1] Neue Aufgabe hinzufügen")
	fmt.Println("[2] Bestehende Aufgabe bearbeiten")
	fmt.Println("[3] Bestehende Aufgabe entfernen")
	fmt.Println("[4] Alle Aufgaben anzeigen")
	fmt.Println("[5] Beenden")

	reader := bufio.NewReader(os.Stdin)

	for {
		fmt.Println("Wählen Sie eine Option: ")
		input, err := reader.ReadString('\n')
		input = strings.TrimSpace(input)
		Model.Check, err = strconv.Atoi(input)

		if err != nil || Model.Check < 1 || Model.Check > 5 {
			fmt.Println("Ungültige Eingabe. Bitte versuchen Sie es erneut.")
		} else {
			SwitchChoose(Model.Check)
			break
		}
	}
}*/
